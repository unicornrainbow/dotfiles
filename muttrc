# Identity, auth, connection
set from = "me@myfreeweb.ru"
set realname = "Grigory V."
set imap_user = "me@myfreeweb.ru"
set smtp_url = "smtp://me@myfreeweb.ru@smtp.gmail.com:587/"
set folder = "imaps://imap.gmail.com:993"
set imap_keepalive = 900

# Folders
set spoolfile = "+INBOX"
set postponed = "+[Gmail]/Drafts"
set trash = "imaps://imap.gmail.com/[Gmail]/Trash"
set header_cache = ~/.mutt/cache/headers
set message_cachedir = ~/.mutt/cache/bodies
set certificate_file = ~/.mutt/certificates

# UI
set move = no
set markers = no
set editor = "emacsclient"
macro pager \cb <pipe-entry>"conkeror"<enter> "Follow URLs with Conkeror"