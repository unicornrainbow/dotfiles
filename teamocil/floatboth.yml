windows:
- name: floatboth
  splits:
  - cmd: cd $CODEDIR/private/floatboth
  - cmd:
    - cd $CODEDIR/private/floatboth
    - nanoc aco & python -c 'import time;time.sleep(2)' && open http://localhost:3000
    height: 10
